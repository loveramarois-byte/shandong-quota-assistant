# 0.8.7 发布说明

- AI 设置收敛为“选择服务商 → 粘贴 Key → 连接并获取模型”三步，补充 DeepSeek、智谱 Key 获取入口和连接后的下一步提示。
- 首屏不再同步读取历史会话，也不再立即争抢 2.7GB 资料库；会话、统计、预热和版本检查按优先级延后到后台执行。
- 鼠标滚轮优先使用事件来源定位滚动区，减少大结果页滚动时反复进行 Windows 指针命中检测。
- 新增自动更新检查：GitHub 优先、Gitee 兜底，每 24 小时最多自动检查一次；发现新版本后在侧栏和“关于”窗口提示。
- 更新功能只打开公开发行页，不会静默下载、安装或收集 API Key。

GitHub 发行版下载：`ShandongQuotaAssistant-Setup-0.8.7.exe`，旁边的 `SHA256SUMS.txt` 用于校验。

本版本继续使用内置结构化山东清单定额资料库。安装包未签名，首次运行可能显示 Windows 未知发布者提示。
